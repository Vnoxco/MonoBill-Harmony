<script schema type="application/json">
    {
        "name": "Image",
        "icon": "image",
        "settings": {
            "alt": {
                "label": "Name",
                "type": "text"
            },
            "image": {
                "label": "Image",
                "type": "image"
            }
        }
    }
</script>

{% from "macros/optimized_image.twig" import optimized_image %}

{{ optimized_image({
    path: schema.settings.image.image.path,
    variants: schema.settings.image.image.variants,
    alt: schema.settings.alt
}) }}
